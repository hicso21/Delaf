import { Text, View } from "react-native";

export default function Frecuency() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text adjustsFontSizeToFit={true}>Frecuency</Text>
    </View>
  );
}